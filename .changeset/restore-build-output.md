---
'@teiler/core': patch
'@teiler/vue': patch
'@teiler/svelte': patch
---

Minify published bundles again and keep `@teiler/core` types external in adapter typings, so `DefaultTheme` augmentation works
